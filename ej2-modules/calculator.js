class calculator {

    static sum(p1, p2) {
        return p1 + p2;
    }

    static substract(p1, p2) {
        return p1 - p2;
    }

    static multiply(p1, p2) {
        return p1 * p2;
    }

    static divide(p1, p2) {
        return p1 / p2;
    }
}

module.exports = calculator;
